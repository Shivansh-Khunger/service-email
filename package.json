{
	"name": "service-email",
	"version": "0.1.0",
	"description": "this is the service-email for iJUJU.",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240320.1",
		"typescript": "^5.0.4",
		"wrangler": "^3.0.0"
	}
}
